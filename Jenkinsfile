pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Webhook triggered build with latest changes.'
                sh 'date'
            }
        }
    }
}
