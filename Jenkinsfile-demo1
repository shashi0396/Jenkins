pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build triggered using Jenkinsfile'
                sh 'ls -la'
                sh 'date'
                echo 'webhook trigger successful - from push event'
            }
        }
    }
}
